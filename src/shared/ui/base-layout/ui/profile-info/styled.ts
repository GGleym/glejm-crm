import {Flex} from '@mtsdengi/uikit-fintech';
import styled from 'styled-components';

const DateWrapper = styled(Flex)`
  background: white;
  padding: 8px 16px;
  border-radius: 16px;
`;

export const Styled = {
  DateWrapper,
};
