import styled from 'styled-components';

const Container = styled.div`
  max-width: 75%;
  margin: 0 auto;
  transform: translateY(-50%);
  top: 50%;
  position: relative;
`;

export const Styled = {
  Container,
};
